import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.iframe',
  appName: 'cs-iframe',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
